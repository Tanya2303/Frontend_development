app.jsx ka parent main.jsx 

app.jsx gaya main.jsx me then main.jsx me h root jo ki h index.html me 


app ka parent main 
main ka parent index.html


app ek he cheez return karta h 
2 cheeze return nhi kar sakte kyuki yeh ek function h toh ek he cheez return kar sakta h

<> </> these are called fragments in react
fragments are used to group a list of children without adding extra nodes to the DOM.
 

 .eslintrc.cjs -> use to make custom rules. 


 complete structure of react app 

 rafce -> react arrow function component export


 component notes 
 -> card.jsx 

props -> props are used to pass data from parent component to child component.
props are read-only.
props are immutable.
props are used to make the component reusable.
props are used to make the component dynamic.       

arrays as a props -> we can pass arrays as props to a component and we can use that array in the component to render the data.
code snippet for passing array as props 
array.map

